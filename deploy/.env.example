# SignalPath deployment configuration
# Copy this file to .env and fill in your values.

# Your GitHub username — this is the account that forked signalpath-core and
# whose GitHub Actions published the image to ghcr.io/<GHCR_ORG>/signalpath.
GHCR_ORG=your-github-username

# Area image tag — the slug you chose when you set up your area, plus "-latest".
# Format: <area-slug>-latest  (e.g. morgan-county-tn-latest, springfield-il-latest)
AREA_TAG=your-area-slug-latest

# Domain name for your SignalPath site.
# - Set to a real hostname (e.g. roadstatus.yourcounty.gov) for automatic TLS via
#   Let's Encrypt (requires the domain to point to this server's public IP first).
# - Set to ":80" to serve HTTP only (use with docker-compose.proxy.yml).
# - Set to "localhost" for local development (self-signed certificate).
DOMAIN=roadstatus.yourcounty.gov

# Admin password for /admin.php (report management) and pla-ng (/phpliteadmin.php).
# Change this before going live. Both tools share this single password.
ADMIN_PASSWORD=changeme
